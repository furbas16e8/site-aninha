/**
 * Main JavaScript - Ponto de entrada
 */

import { initNavbar } from './components/navbar.js';

document.addEventListener('DOMContentLoaded', () => {
  initNavbar();
  console.log('🌳 EcoEngine initialized');
});
